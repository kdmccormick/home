#/usr/bin/env bash

# PRIVATE PROFILE OVERRIDES
# Not version controlled.
# This is sourced by every login shell!
# It is not sourced by non-login bash shells.

# Optionally override these.
#export KI_USER=
#export KI_DIST=
#export KI_PROFILE=
#export KI_EMAIL=
#export KI_FULLNAME=""

# MUST override these
echo "You forgot to override things in .profile_private!" && exit 1  # <- remove this when ready
export KI_SSH_PASSPHRASE="SET-ME!"

